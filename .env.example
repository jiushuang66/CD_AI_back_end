# CD AI 后端 环境变量示例
# 复制本文件为 .env 并根据需要修改值

PROJECT_NAME=CD AI Backend
VERSION=0.1.0
DESCRIPTION=CD AI Backend API

# CORS 来源（逗号分隔，使用 * 表示全部）
CORS_ORIGINS=*

HOST=0.0.0.0
PORT=8000
DEBUG=false
RELOAD=false

# 如果提供了 DATABASE_URL，则会覆盖下面的 MYSQL_* 配置
# 示例：DATABASE_URL=mysql+pymysql://user:pass@127.0.0.1:3306/cd_ai_db?charset=utf8mb4
DATABASE_URL=

# MySQL 连接参数（当 DATABASE_URL 为空时使用）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=cd_ai_db

# JWT / 认证 配置
ACCESS_TOKEN_EXPIRE_MINUTES=60
SECRET_KEY=change-me
ALGORITHM=HS256
