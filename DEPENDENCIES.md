已由维护者请求“删除冗余文件”。此文件内容已清空并由自动化脚本标记为已移除。

如果你需要恢复详细依赖说明，请使用 `requirements.txt` 或版本控制历史记录还原本文件。

已移除项（按用户请求）：
- `logs/` 目录内的运行日志（已清空）
- `src/` 目录（冗余副本，已清理）
- `DEPENDENCIES.md`（本文件）

操作时间：2026-01-11
  - 用于请求/响应模型定义
  - 强类型校验
  - 自动数据验证和序列化

### 认证
- **PyJWT** ≥2.9.0
  - 生成/解析 JWT Token
  - 使用 HS256 算法

### 密码加密
- **bcrypt** ≥4.2.0
  - 用户密码哈希存储
  - 安全的密码加密算法

### HTTP客户端
- **requests** ≥2.32.0
  - 用于同步调用外部 AI 服务
  - 因无 Celery，采用后台线程或直接阻塞调用

### 配置管理
- **python-dotenv** ≥1.2.1
  - 从 .env 加载数据库 URL、OSS 密钥等配置

### ASGI服务器
- **uvicorn** ≥0.38.0
  - 开发与生产部署运行器
  - 支持热重载

### 文件上传
- **python-multipart** ≥0.0.20
  - 支持 FastAPI 的 UploadFile
  - 处理文件上传请求

### 图像处理
- **Pillow** ≥12.0.0
  - 未来扩展图表预览时使用
  - V1.0 可暂不启用

## 其他依赖
- **Alembic** 1.12.1 - 数据库迁移工具
- **loguru** 0.7.2 - 日志库
- **python-dateutil** 2.8.2 - 日期时间工具

## 安装

```bash
pip install -r requirements.txt
```

## 注意事项

1. 确保 MySQL 8.0+ 已安装并运行
2. 创建数据库时使用 InnoDB 引擎
3. JWT Secret Key 在生产环境中必须使用强随机密钥
4. 所有版本号使用 `>=` 以确保兼容性

